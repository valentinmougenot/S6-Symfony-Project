<div class="flex items-center justify-center min-h-screen bg-gray-100 py-12 px-4 sm:px-6 lg:px-8 w-full">
    <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold mb-6 text-center">Inscription échouée</h1>
        <p>Bonjour {{ user.getFirstname() }} {{ user.getLastname() }},</p>
        <p>Vous n'avez pas pu vous inscrire à l'événement "{{ event.getTitle() }}" qui aura lieu le {{ event.getDate()|date('d/m/Y') }} à {{ event.getDate()|date('H:i') }}.</p>
        <p>Description : "{{ event.getDescription() }}"</p>
        <p>Il se peut qu'il n'y ait plus de place disponible pour cet événement.</p>
        <p>Veuillez nous excuser pour ce désagrément.</p>
    </div>
</div>